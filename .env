DB_HOST=localhost
DB_NAME=haile
DB_USER=root
DB_PASS=

# BASE_URL is auto-detected from the request (works on localhost, LAN IP, or live domain)
# Only set this if you need to force a specific URL (e.g. production with HTTPS)
# BASE_URL=https://yourdomain.com/pro

MAX_IMAGE_SIZE=31457280
MAX_VIDEO_SIZE=209715200

SESSION_TIMEOUT=7200
